name: redisoid
version: 0.2

authors:
  - Konstantin Makarchev kostya27@gmail.com

dependencies:
  pool:
    github: ysbaddaden/pool
  redis-reconnect:
    github: kostya/redis-reconnect
    version: ">= 0.2"

license: MIT
